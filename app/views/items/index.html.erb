<h1>Todo list</h1>
<p><%= link_to 'New Item', new_item_path %></p>
<% @items.each do |item| %>
	<h2><%= item.name %></h2>
	<p><%= item.description %></p>
	<p><%= item.complete %></p>
	<%= link_to 'Show item', item_path(item) %>
<% end %>



