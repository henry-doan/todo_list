<h1>Add a new item</h1>

<%= form_for @item do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.label :complete %>
	<%= f.check_box :complete %>
	<%= f.submit%>
<% end %>

